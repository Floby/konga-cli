#!/usr/bin/env node
// vi: set ft=javascript

var program = require('commander');
var apis = require('../commands/apis');

program
  .command('api', 'API operations')
  .command('consumer', 'Consumer operations')
  .command('plugin', 'Plugin operations')

program.parse(process.argv)
